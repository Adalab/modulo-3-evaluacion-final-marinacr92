import logoHP from '../images/logoHP.png';

const Header = () => {
  return (
    <header>
      <div>
        <img src={logoHP} alt="Logo de Harry Potter" />
      </div>
    </header>
  );
};

export default Header;
